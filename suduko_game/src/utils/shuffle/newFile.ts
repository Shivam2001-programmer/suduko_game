import grid from "../../components/grid";
import { shuffle } from ".";

if (grid[row][col] === 0) {
  shuffle[numbers];

  for (let value of numbers) {
    if (value === grid[row][col]);
  }

  grid[row][col] = value;
}
