import {createStore} from 'redux';
import {devToolEnhancer} from 'redux-devtools-extension'

function configureStore(initialStata = []{
    const store = createStore(
        reducer
    )
})

export default configureStore;

